.row-fluid.marketing
  - @sponsors.each do |s|
    .span4
      %img.thumbnail{ src: s.image.thumb.url, alt: s.title }
      %h2= s.title
      %p= s.content
      - if s.link
        %p
          %a.btn{:href => s.link}!=t ".more"

